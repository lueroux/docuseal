<% issues.each do |issue| %>
  <div class="alert <%= issue[:type] == 'requires' ? 'alert-error' : issue[:type] == 'excludes' ? 'alert-warning' : 'alert-info' %>">
    <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
      <% if issue[:type] == 'requires' || issue[:type] == 'excludes' %>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
      <% else %>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      <% end %>
    </svg>
    <div>
      <h3 class="font-bold"><%= issue[:type].titleize %></h3>
      <div class="text-sm"><%= issue[:message] %></div>
    </div>
    <% if issue[:action] == 'add' || issue[:action] == 'suggest' %>
      <button class="btn btn-sm">Add <%= issue[:sku] %></button>
    <% end %>
  </div>
<% end %>
